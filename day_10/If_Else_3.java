package day_10;

public class If_Else_3 {

	public static void main(String[] args) {
	/*
		int a = 100;
		int b = 20;
		
		String operator = "/";
		
		int total = 0;
		
		if (operator == "+")
			total = a + b;
		if (operator == "-")
			total = a - b;
		if (operator == "*") 
			total = a * b;
		if (operator == "/")
			total = a / b;
		
		System.out.println(total);
		
		
//		Write an if statement that assigns 5 to x when y is 20
		
		int x;
		int y = 20;
		if(y == 20) {
			x = 5;
		System.out.println(x);
		}
		
		//even the sysout needs to be in the {} or else the if statement doesn't work conditionally
		
		
	//Write an if statement that multiples payrate by 1.5 if hours is greater than 40
		
		int hours;
		double payrate = 500;
		hours = 40;
		if (hours > 40) {
			payrate = payrate * 1.5;
		}
		System.out.println("Your payrate is " + payrate);
		*/
		
		//Write an if statement that sets the variable "fees" to 50 if the boolean variable "max" is true
		
		int fees = 0;
		boolean max = true;
		
		if (max == true)
			fees = 50;
		
		System.out.println(fees);
		
	}

}
